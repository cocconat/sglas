#ifndef MEASURE_INCLUDE
#define MEASURE_INCLUDE

namespace measure{
template <class It>
int magnetization(It begin,It begin);
double energy(const std::array<int,SIZE>& reticle,const std::array<double);
}
#endif

